import 'package:flutter/material.dart';

  ///colors:
  const greenAccent = Color.fromARGB(255, 46, 125, 50);
  const golden = Color(0XFFFFD700);
  const redAccent = Color(0XFFFF5733);

  const activeRatingBarColor = Color(0XFF1F8CFF);
  const inactiveRatingBarColor = Color(0XFF252634);

  const primaryTextColor = Color(0xFF3A96FF);

  ///shared preferences keys:
  const String taskTitlesListKey = 'task_titles_list';
  const String taskIsCompletedListKey = 'task_is_selected_list';